{
  "name": "biblia-de-estudo",
  "version": "2.3.0",
  "private": true,
  "description": "Bíblia de Estudo — app web/PWA preparado para Android com Capacitor.",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:build": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:build:release": "cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  }
}
